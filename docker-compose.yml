# ./docker-compose.yml
#version: "3.9"

services:
  app:
    build: .
    image: quant-meta-hybrid-trader:latest
    container_name: quant-meta-hybrid-trader
    working_dir: /usr/src/app

    # .env を環境変数として注入（API_KEY / API_SECRETとか）


    # ホストのソースをコンテナにマウント
    volumes:
      - .:/usr/src/app

    # 必要なら外部公開ポート（Jupyterとか使うとき）
    # ports:
    #   - "8888:8888"

    # 開発中はコンテナを生かしっぱなしにしておく
    tty: true
    stdin_open: true
    command: tail -f /dev/null
